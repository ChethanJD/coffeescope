import type { CSSProperties } from "react";

export const chartTooltipStyle: CSSProperties = {
  background: "#161616",
  border: "1px solid rgba(255,255,255,0.08)",
  borderRadius: 12,
  fontSize: 13,
  color: "#fff",
};

export const axisTickStyle = { fill: "#ffffff66", fontSize: 12 };
